# Generated from sdlc.yml by sdlc generate. DO NOT EDIT.
# System lifecycle phase: documentation@1
# Source digest: sha256:0775962e45bf02382119b7a1bc11bc3941d85695dd530be5333843b281c0479b
name: SDLC / Documentation
on:
  repository_dispatch:
    types:
      - sdlc.phase.documentation
  workflow_run:
    workflows:
      - CI
    types:
      - completed
concurrency:
  group: sdlc-documentation-${{ github.event.issue.number || github.event.client_payload.issue }}
  cancel-in-progress: false
jobs:
  phase:
    if: ${{ github.event_name == 'repository_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: read
      pull-requests: read
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          ref: ${{ github.event.client_payload.head_sha || github.event.workflow_run.head_sha || github.sha }}
      - uses: pnpm/action-setup@v6
        with:
          version: 11.2.2
          dest: ${{ runner.temp }}/setup-pnpm
      - uses: actions/setup-node@v7
        with:
          node-version: 24.11.0
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Run Documentation
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: pnpm exec sdlc phase run documentation --config sdlc.yml --event "$GITHUB_EVENT_PATH" --output "${{ runner.temp }}/sdlc-documentation-result.json" --source-sha "${{ github.event.client_payload.head_sha || github.event.workflow_run.head_sha || github.sha }}"
      - name: Verify phase result
        run: pnpm exec sdlc phase verify documentation --config sdlc.yml --event "$GITHUB_EVENT_PATH" --result "${{ runner.temp }}/sdlc-documentation-result.json" --source-sha "${{ github.event.client_payload.head_sha || github.event.workflow_run.head_sha || github.sha }}"
      - name: Upload phase evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@v6
        with:
          name: sdlc-documentation-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/sdlc-documentation-result.json
          if-no-files-found: error
  advance:
    needs:
      - phase
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
      actions: read
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          ref: ${{ github.event.client_payload.head_sha || github.event.workflow_run.head_sha || github.sha }}
      - uses: pnpm/action-setup@v6
        with:
          version: 11.2.2
          dest: ${{ runner.temp }}/setup-pnpm
      - uses: actions/setup-node@v7
        with:
          node-version: 24.11.0
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - uses: actions/download-artifact@v7
        with:
          name: sdlc-documentation-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}
      - name: Advance lifecycle
        env:
          GH_TOKEN: ${{ github.token }}
        run: pnpm exec sdlc phase advance documentation --config sdlc.yml --event "$GITHUB_EVENT_PATH" --result "${{ runner.temp }}/sdlc-documentation-result.json" --source-sha "${{ github.event.client_payload.head_sha || github.event.workflow_run.head_sha || github.sha }}"
