# Generated from sdlc.yml by sdlc generate. DO NOT EDIT.
# Source digest: sha256:32a9602d67f409157264aaf1180c30e172c7f3f7b0699dbbe83a1773c37e5415
name: Example delivery graph
on:
  workflow_dispatch: {}
jobs:
  scout:
    name: Check viability
    outputs:
      sdlc_result: ${{ steps['assess'].outputs.result }}
    runs-on: ubuntu-latest
    permissions:
      packages: read
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@v7
        with:
          node-version: 24.11.0
          cache: npm
          registry-url: https://npm.pkg.github.com
          scope: "@lance-ventures"
      - run: npm ci --no-audit --no-fund
        env:
          NODE_AUTH_TOKEN: ${{ github.token }}
      - id: assess
        name: Assess the request
        env:
          SDLC_RELEASE_TOKEN: ${{ github.token }}
        run: npm exec --offline -- sdlc execute --config sdlc.yml --workflow delivery --job scout --step assess --event "$GITHUB_EVENT_PATH" --github-output "$GITHUB_OUTPUT" --source-sha "${{ github.event.inputs.head_sha || github.event.workflow_run.head_sha || github.event.client_payload.head_sha || github.sha }}"
      - id: compile
        name: Compile the application
        run: npm run build
  brief:
    name: Establish the contract
    needs:
      - scout
    outputs:
      sdlc_result: ${{ steps['define'].outputs.result }}
    runs-on: ubuntu-latest
    permissions:
      packages: read
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@v7
        with:
          node-version: 24.11.0
          cache: npm
          registry-url: https://npm.pkg.github.com
          scope: "@lance-ventures"
      - run: npm ci --no-audit --no-fund
        env:
          NODE_AUTH_TOKEN: ${{ github.token }}
      - id: define
        name: Define expected behavior
        env:
          SDLC_RELEASE_TOKEN: ${{ github.token }}
          SDLC_UPSTREAM_RESULT_0: ${{ needs['scout'].outputs.sdlc_result }}
        run: npm exec --offline -- sdlc execute --config sdlc.yml --workflow delivery --job brief --step define --event "$GITHUB_EVENT_PATH" --github-output "$GITHUB_OUTPUT" --upstream-result "scout=$SDLC_UPSTREAM_RESULT_0" --source-sha "${{ github.event.inputs.head_sha || github.event.workflow_run.head_sha || github.event.client_payload.head_sha || github.sha }}"
      - id: docs
        name: Check consumer documentation
        run: npm run docs:check
  map:
    name: Choose the route
    needs:
      - brief
    outputs:
      sdlc_result: ${{ steps['organize'].outputs.result }}
    runs-on: ubuntu-latest
    permissions:
      packages: read
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@v7
        with:
          node-version: 24.11.0
          cache: npm
          registry-url: https://npm.pkg.github.com
          scope: "@lance-ventures"
      - run: npm ci --no-audit --no-fund
        env:
          NODE_AUTH_TOKEN: ${{ github.token }}
      - id: organize
        name: Organize the work
        env:
          SDLC_RELEASE_TOKEN: ${{ github.token }}
          SDLC_UPSTREAM_RESULT_0: ${{ needs['brief'].outputs.sdlc_result }}
        run: npm exec --offline -- sdlc execute --config sdlc.yml --workflow delivery --job map --step organize --event "$GITHUB_EVENT_PATH" --github-output "$GITHUB_OUTPUT" --upstream-result "brief=$SDLC_UPSTREAM_RESULT_0" --source-sha "${{ github.event.inputs.head_sha || github.event.workflow_run.head_sha || github.event.client_payload.head_sha || github.sha }}"
      - id: compile
        name: Compile the planned surface
        run: npm run build
  shape:
    name: Define the structure
    needs:
      - map
    outputs:
      sdlc_result: ${{ steps['structure'].outputs.result }}
    runs-on: ubuntu-latest
    permissions:
      packages: read
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@v7
        with:
          node-version: 24.11.0
          cache: npm
          registry-url: https://npm.pkg.github.com
          scope: "@lance-ventures"
      - run: npm ci --no-audit --no-fund
        env:
          NODE_AUTH_TOKEN: ${{ github.token }}
      - id: structure
        name: Define the implementation structure
        env:
          SDLC_RELEASE_TOKEN: ${{ github.token }}
          SDLC_UPSTREAM_RESULT_0: ${{ needs['map'].outputs.sdlc_result }}
        run: npm exec --offline -- sdlc execute --config sdlc.yml --workflow delivery --job shape --step structure --event "$GITHUB_EVENT_PATH" --github-output "$GITHUB_OUTPUT" --upstream-result "map=$SDLC_UPSTREAM_RESULT_0" --source-sha "${{ github.event.inputs.head_sha || github.event.workflow_run.head_sha || github.event.client_payload.head_sha || github.sha }}"
      - id: compile
        name: Compile the designed surface
        run: npm run build
  look:
    name: Verify presentation
    needs:
      - shape
    outputs:
      sdlc_result: ${{ steps['present'].outputs.result }}
    runs-on: ubuntu-latest
    permissions:
      packages: read
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@v7
        with:
          node-version: 24.11.0
          cache: npm
          registry-url: https://npm.pkg.github.com
          scope: "@lance-ventures"
      - run: npm ci --no-audit --no-fund
        env:
          NODE_AUTH_TOKEN: ${{ github.token }}
      - id: present
        name: Define the presentation contract
        env:
          SDLC_RELEASE_TOKEN: ${{ github.token }}
          SDLC_UPSTREAM_RESULT_0: ${{ needs['shape'].outputs.sdlc_result }}
        run: npm exec --offline -- sdlc execute --config sdlc.yml --workflow delivery --job look --step present --event "$GITHUB_EVENT_PATH" --github-output "$GITHUB_OUTPUT" --upstream-result "shape=$SDLC_UPSTREAM_RESULT_0" --source-sha "${{ github.event.inputs.head_sha || github.event.workflow_run.head_sha || github.event.client_payload.head_sha || github.sha }}"
      - id: visual
        name: Run the visual contract
        run: npm run visual
  build:
    name: Construct the change
    needs:
      - shape
      - look
    outputs:
      sdlc_result: ${{ steps['construct'].outputs.result }}
    runs-on: ubuntu-latest
    permissions:
      packages: read
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@v7
        with:
          node-version: 24.11.0
          cache: npm
          registry-url: https://npm.pkg.github.com
          scope: "@lance-ventures"
      - run: npm ci --no-audit --no-fund
        env:
          NODE_AUTH_TOKEN: ${{ github.token }}
      - id: construct
        name: Construct the requested behavior
        env:
          SDLC_RELEASE_TOKEN: ${{ github.token }}
          SDLC_UPSTREAM_RESULT_0: ${{ needs['shape'].outputs.sdlc_result }}
          SDLC_UPSTREAM_RESULT_1: ${{ needs['look'].outputs.sdlc_result }}
        run: npm exec --offline -- sdlc execute --config sdlc.yml --workflow delivery --job build --step construct --event "$GITHUB_EVENT_PATH" --github-output "$GITHUB_OUTPUT" --upstream-result "shape=$SDLC_UPSTREAM_RESULT_0" --upstream-result "look=$SDLC_UPSTREAM_RESULT_1" --source-sha "${{ github.event.inputs.head_sha || github.event.workflow_run.head_sha || github.event.client_payload.head_sha || github.sha }}"
      - id: test
        name: Test the constructed behavior
        run: npm test
  explain:
    name: Explain the change
    needs:
      - build
    outputs:
      sdlc_result: ${{ steps['describe'].outputs.result }}
    runs-on: ubuntu-latest
    permissions:
      packages: read
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@v7
        with:
          node-version: 24.11.0
          cache: npm
          registry-url: https://npm.pkg.github.com
          scope: "@lance-ventures"
      - run: npm ci --no-audit --no-fund
        env:
          NODE_AUTH_TOKEN: ${{ github.token }}
      - id: describe
        name: Describe the resulting behavior
        env:
          SDLC_RELEASE_TOKEN: ${{ github.token }}
          SDLC_UPSTREAM_RESULT_0: ${{ needs['build'].outputs.sdlc_result }}
        run: npm exec --offline -- sdlc execute --config sdlc.yml --workflow delivery --job explain --step describe --event "$GITHUB_EVENT_PATH" --github-output "$GITHUB_OUTPUT" --upstream-result "build=$SDLC_UPSTREAM_RESULT_0" --source-sha "${{ github.event.inputs.head_sha || github.event.workflow_run.head_sha || github.event.client_payload.head_sha || github.sha }}"
      - id: docs
        name: Verify the documentation contract
        run: npm run docs:check
  prove:
    name: Prove the change
    needs:
      - build
    outputs:
      sdlc_result: ${{ steps['exercise'].outputs.result }}
    runs-on: ubuntu-latest
    permissions:
      packages: read
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@v7
        with:
          node-version: 24.11.0
          cache: npm
          registry-url: https://npm.pkg.github.com
          scope: "@lance-ventures"
      - run: npm ci --no-audit --no-fund
        env:
          NODE_AUTH_TOKEN: ${{ github.token }}
      - id: exercise
        name: Exercise the resulting behavior
        env:
          SDLC_RELEASE_TOKEN: ${{ github.token }}
          SDLC_UPSTREAM_RESULT_0: ${{ needs['build'].outputs.sdlc_result }}
        run: npm exec --offline -- sdlc execute --config sdlc.yml --workflow delivery --job prove --step exercise --event "$GITHUB_EVENT_PATH" --github-output "$GITHUB_OUTPUT" --upstream-result "build=$SDLC_UPSTREAM_RESULT_0" --source-sha "${{ github.event.inputs.head_sha || github.event.workflow_run.head_sha || github.event.client_payload.head_sha || github.sha }}"
      - id: test
        name: Run the deterministic test suite
        run: npm test
  judge:
    name: Apply the release gate
    needs:
      - explain
      - prove
    outputs:
      sdlc_result: ${{ steps['evaluate'].outputs.result }}
    runs-on: ubuntu-latest
    permissions:
      packages: read
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@v7
        with:
          node-version: 24.11.0
          cache: npm
          registry-url: https://npm.pkg.github.com
          scope: "@lance-ventures"
      - run: npm ci --no-audit --no-fund
        env:
          NODE_AUTH_TOKEN: ${{ github.token }}
      - id: evaluate
        name: Evaluate release readiness
        env:
          SDLC_RELEASE_TOKEN: ${{ github.token }}
          SDLC_UPSTREAM_RESULT_0: ${{ needs['explain'].outputs.sdlc_result }}
          SDLC_UPSTREAM_RESULT_1: ${{ needs['prove'].outputs.sdlc_result }}
        run: npm exec --offline -- sdlc execute --config sdlc.yml --workflow delivery --job judge --step evaluate --event "$GITHUB_EVENT_PATH" --github-output "$GITHUB_OUTPUT" --upstream-result "explain=$SDLC_UPSTREAM_RESULT_0" --upstream-result "prove=$SDLC_UPSTREAM_RESULT_1" --source-sha "${{ github.event.inputs.head_sha || github.event.workflow_run.head_sha || github.event.client_payload.head_sha || github.sha }}"
      - id: check
        name: Run every consumer check
        run: npm run check
  ship:
    name: Exercise delivery
    needs:
      - judge
    outputs:
      sdlc_result: ${{ steps['deliver'].outputs.result }}
    runs-on: ubuntu-latest
    permissions:
      packages: read
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@v7
        with:
          node-version: 24.11.0
          cache: npm
          registry-url: https://npm.pkg.github.com
          scope: "@lance-ventures"
      - run: npm ci --no-audit --no-fund
        env:
          NODE_AUTH_TOKEN: ${{ github.token }}
      - id: deliver
        name: Validate delivery behavior
        env:
          SDLC_RELEASE_TOKEN: ${{ github.token }}
          SDLC_UPSTREAM_RESULT_0: ${{ needs['judge'].outputs.sdlc_result }}
        run: npm exec --offline -- sdlc execute --config sdlc.yml --workflow delivery --job ship --step deliver --event "$GITHUB_EVENT_PATH" --github-output "$GITHUB_OUTPUT" --upstream-result "judge=$SDLC_UPSTREAM_RESULT_0" --source-sha "${{ github.event.inputs.head_sha || github.event.workflow_run.head_sha || github.event.client_payload.head_sha || github.sha }}"
      - id: smoke
        name: Smoke test the preview
        run: npm run smoke
  watch:
    name: Exercise operation
    needs:
      - ship
    outputs:
      sdlc_result: ${{ steps['observe'].outputs.result }}
    runs-on: ubuntu-latest
    permissions:
      packages: read
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@v7
        with:
          node-version: 24.11.0
          cache: npm
          registry-url: https://npm.pkg.github.com
          scope: "@lance-ventures"
      - run: npm ci --no-audit --no-fund
        env:
          NODE_AUTH_TOKEN: ${{ github.token }}
      - id: observe
        name: Validate operational behavior
        env:
          SDLC_RELEASE_TOKEN: ${{ github.token }}
          SDLC_UPSTREAM_RESULT_0: ${{ needs['ship'].outputs.sdlc_result }}
        run: npm exec --offline -- sdlc execute --config sdlc.yml --workflow delivery --job watch --step observe --event "$GITHUB_EVENT_PATH" --github-output "$GITHUB_OUTPUT" --upstream-result "ship=$SDLC_UPSTREAM_RESULT_0" --source-sha "${{ github.event.inputs.head_sha || github.event.workflow_run.head_sha || github.event.client_payload.head_sha || github.sha }}"
      - id: health
        name: Check preview health
        run: npm run smoke
  tend:
    name: Exercise maintenance
    needs:
      - watch
    outputs:
      sdlc_result: ${{ steps['maintain'].outputs.result }}
    runs-on: ubuntu-latest
    permissions:
      packages: read
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@v7
        with:
          node-version: 24.11.0
          cache: npm
          registry-url: https://npm.pkg.github.com
          scope: "@lance-ventures"
      - run: npm ci --no-audit --no-fund
        env:
          NODE_AUTH_TOKEN: ${{ github.token }}
      - id: maintain
        name: Validate maintenance behavior
        env:
          SDLC_RELEASE_TOKEN: ${{ github.token }}
          SDLC_UPSTREAM_RESULT_0: ${{ needs['watch'].outputs.sdlc_result }}
        run: npm exec --offline -- sdlc execute --config sdlc.yml --workflow delivery --job tend --step maintain --event "$GITHUB_EVENT_PATH" --github-output "$GITHUB_OUTPUT" --upstream-result "watch=$SDLC_UPSTREAM_RESULT_0" --source-sha "${{ github.event.inputs.head_sha || github.event.workflow_run.head_sha || github.event.client_payload.head_sha || github.sha }}"
      - id: boundary
        name: Check the package boundary
        run: npm run maintenance
  finish:
    name: Close the graph
    needs:
      - tend
    outputs:
      sdlc_result: ${{ steps['close'].outputs.result }}
    runs-on: ubuntu-latest
    permissions:
      packages: read
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@v7
        with:
          node-version: 24.11.0
          cache: npm
          registry-url: https://npm.pkg.github.com
          scope: "@lance-ventures"
      - run: npm ci --no-audit --no-fund
        env:
          NODE_AUTH_TOKEN: ${{ github.token }}
      - id: close
        name: Validate completion behavior
        env:
          SDLC_RELEASE_TOKEN: ${{ github.token }}
          SDLC_UPSTREAM_RESULT_0: ${{ needs['tend'].outputs.sdlc_result }}
        run: npm exec --offline -- sdlc execute --config sdlc.yml --workflow delivery --job finish --step close --event "$GITHUB_EVENT_PATH" --github-output "$GITHUB_OUTPUT" --upstream-result "tend=$SDLC_UPSTREAM_RESULT_0" --source-sha "${{ github.event.inputs.head_sha || github.event.workflow_run.head_sha || github.event.client_payload.head_sha || github.sha }}"
      - id: check
        name: Run the final consumer proof
        run: npm run check
