# Generated from sdlc.yml by sdlc generate. DO NOT EDIT.
# System lifecycle phase: quality@1
# Source digest: sha256:20b8d327e2a3c80e481f16b59b1a779f2d26fc880d54db60273379b82fe00b73
name: SDLC / Quality
on:
  repository_dispatch:
    types:
      - sdlc.phase.quality
concurrency:
  group: sdlc-quality-${{ github.event.issue.number || github.event.client_payload.issue }}
  cancel-in-progress: false
jobs:
  phase:
    runs-on: ubuntu-latest
    permissions:
      packages: read
      contents: read
      issues: read
      pull-requests: read
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          ref: ${{ github.event.client_payload.head_sha || github.event.workflow_run.head_sha || github.sha }}
      - uses: actions/setup-node@v7
        with:
          node-version: 24.11.0
          cache: npm
          registry-url: https://npm.pkg.github.com
          scope: "@lance-ventures"
      - run: npm ci --no-audit --no-fund
        env:
          NODE_AUTH_TOKEN: ${{ github.token }}
      - name: Run Quality
        run: npx --no-install sdlc phase run quality --config sdlc.yml --event "$GITHUB_EVENT_PATH" --output "${{ runner.temp }}/sdlc-quality-result.json" --source-sha "${{ github.event.client_payload.head_sha || github.event.workflow_run.head_sha || github.sha }}"
      - name: Verify phase result
        run: npx --no-install sdlc phase verify quality --config sdlc.yml --event "$GITHUB_EVENT_PATH" --result "${{ runner.temp }}/sdlc-quality-result.json" --source-sha "${{ github.event.client_payload.head_sha || github.event.workflow_run.head_sha || github.sha }}"
      - name: Upload phase evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@v6
        with:
          name: sdlc-quality-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/sdlc-quality-result.json
          if-no-files-found: error
  advance:
    needs:
      - phase
    runs-on: ubuntu-latest
    permissions:
      packages: read
      contents: write
      actions: read
      checks: read
      issues: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          ref: ${{ github.event.client_payload.head_sha || github.event.workflow_run.head_sha || github.sha }}
      - uses: actions/setup-node@v7
        with:
          node-version: 24.11.0
          cache: npm
          registry-url: https://npm.pkg.github.com
          scope: "@lance-ventures"
      - run: npm ci --no-audit --no-fund
        env:
          NODE_AUTH_TOKEN: ${{ github.token }}
      - uses: actions/download-artifact@v7
        with:
          name: sdlc-quality-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}
      - name: Advance lifecycle
        env:
          GH_TOKEN: ${{ github.token }}
        run: npx --no-install sdlc phase advance quality --config sdlc.yml --event "$GITHUB_EVENT_PATH" --result "${{ runner.temp }}/sdlc-quality-result.json" --source-sha "${{ github.event.client_payload.head_sha || github.event.workflow_run.head_sha || github.sha }}"
